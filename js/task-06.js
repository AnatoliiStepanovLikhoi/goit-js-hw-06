


































const colorOption = [
    { label: 'red', color: '#F44336' },
    { label: 'green', color: '#4CAF50' },
    { label: 'blue', color: '#2196F3' },
    { label: 'grey', color: '#607D88'},
]

// const buttonEl = document.createElement('button');
// buttonEl.type = 'button';
// buttonEl.textContent = colorOption[1].label;
// buttonEl.style.backgroundColor = colorOption[1].color;

// console.log(buttonEl);

const divRef = document.querySelector('#counter')

// const elements = colorOption.map(option => {
//     const buttonEl = document.createElement('button');
//     buttonEl.type = 'button';

//     buttonEl.textContent = option.label;
//     buttonEl.style.backgroundColor = option.color;
    
//     return buttonEl
// });

// console.log(elements)

// const elements = []

// for (let i = 0; i< colorOption.length; i += 1) {
//     const option = colorOption[i];

//     const buttonEl = document.createElement('button');
// buttonEl.type = 'button';
// buttonEl.textContent = option.label;
// buttonEl.style.backgroundColor = option.color;

//     console.log(buttonEl);
    
//     elements.push(buttonEl)
    
// }

// console.log(elements)

// divRef.append(...elements)

// const makeColorOption = options => {
//     return options.map(option => {
//         const buttonEl = document.createElement('button');
//         buttonEl.type = 'button';

//         buttonEl.textContent = option.label;
//         buttonEl.style.backgroundColor = option.color;
    
//         return buttonEl
//     })
// }

// const elements = makeColorOption(colorOption)

// console.log(elements)
// divRef.append(...elements)